{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "storybook-dist"
      }
    }
  ],
  "scripts": {
    "build": "npm run build-storybook"
  }
}